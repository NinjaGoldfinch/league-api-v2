APP_NAME=League API
APP_ENV=development
RIOT_API_KEY=
DEFAULT_PLATFORM_ROUTE=oc1
DEFAULT_REGIONAL_ROUTE=sea

# Jobs are process-local and in-memory in this stage.
# There is no Redis, database, persistent cache, or external worker setting yet.
